  layout {
  match: League_search (this)
  mode (Summary)

  content {
    section {
      content {
        for-each (this.image_URL.url)
        {
          as (x) {
           image {
            object-fit (Contain)
             url("#{value(this.image_URL.url)}")
            } 

            paragraph {
              value("#{value(this.start_date)}")
              style(Detail_M)
            }

          }
        }       
      }
    }
  }
}
