layout {
  match: Leauge_search (this)
  mode (Summary)

  content {
    section {
      content {
        ////////find image NOT WORKING
          image {
          object-fit (Contain)
          background-color (White)
           url("#{value(this.image_URL.url)}")
          } 
      }
    }
  }
}
